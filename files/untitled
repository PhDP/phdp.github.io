typedef struct {
  double *elems;
  size_t ncols;
  size_t nrows;
} matrix;

void random_mat(matrix *m, size_t cols, size_t rows, size_t seed) {
};

void print_mat(matrix *m, std...) {
  for () {
    for () {
    }
  }
};

void multiply(matrix *a, matrix *b, matrix *c) {
  c->ncols = a->nrows;
  c->nrows = b->ncols;

  for () {
    for () {
      for () {
      }
    }
  }
}
